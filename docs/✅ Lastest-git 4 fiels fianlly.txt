✅ ✅ POWERHELL VERSION (WORKING)

Use ; instead of && 👇:-

# Final Git Bash Command:-
git add .; git commit -m "🚀 feat: full metadata pipeline (CI + CD + UI)" `
-m "🟢 Added tag-based version tracking" `
-m "🟢 Added commit SHA (short + full) support" `
-m "🟢 Added commit title + detailed body parsing" `
-m "🟢 Added build_info.json generation in CI" `
-m "🟢 Fixed Dockerfile to preserve CI metadata (removed overwrite bug)" `
-m "🟢 Integrated metadata into Flask health UI" `
-m "🟢 Enhanced CD email with tag + commit + details" `
-m "🟢 Added fallback: 'No details available' when no commit body"; `
git push

=====================================================================================


git add .; git commit -m "🚀 fix: final CI/CD + metadata pipeline stable" -m "🟢 Fixed build_info.json generation in CI" -m "🟢 Removed Docker overwrite bug for metadata" -m "🟢 Ensured tag, commit, title, body flow correctly to UI" -m "🟢 Added --no-cache Docker build for clean deploy" -m "🟢 Stabilized Render health check deployment flow"; git push
